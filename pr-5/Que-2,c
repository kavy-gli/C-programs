#include <stdio.h>

int main() {
    
    int arr[100], i, n , max;

    printf("Enter the number of elements: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("\nThis is your array:\n");
    for(i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
   max=arr[0];
   for(i=0;i<n;i++)
   {
       if(arr[i]>max)
       {
       max=arr[i];    
       }
   }
   printf("\nThis is the greatest no of your array %d",max);
    return 0;
}
